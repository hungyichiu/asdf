<% if @candidate %>

  <h1> <%= @candidate.name %> </h1>
  <ul>
    <li>得票數：<%=@candidate.vote%></li> 
    <li>學歷：<%= @candidate.degree %></li>
    <li>年紀：<%= @candidate.age %></li>
    <li>黨派：<%= @candidate.party %></li>
    <li>政見：<%= @candidate.policy %></li>
  </ul>
<%else%>
  <h1>U can't see me</h1>
<% end %>

<%= link_to '候選人列表', candidates_path, class: 'btn btn-primary btn-lg'  %>